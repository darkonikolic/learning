# Common corrections

Staging area for repeated corrections. When confidence is high, a candidate graduates to a real `.mdc` rule (logged in `decision_log.md`), then is removed here.

Format: `correction — count — status`

- "I didn't want you to fix anything, just tell me in chat if there are problems" — 1 — covered by `reviewer` + `review-work` (chat-only default).
- (add new corrections here as they recur)
