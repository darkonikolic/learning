# Unit 11 — `order-system/` persistence capstone stitching failures intentionally

Mandatory lifecycle coverage sketch:

```
create order
pay order
cancel order
list orders (+ filtered variants optional)
```

## Fault injection rehearsals (document recovery reasoning)

Craft scenarios:

```
transaction rollback correctness after nested failure invocation
locking race resurfacing interplay lessons unit6 repairs verification
purposefully malformed / conflicting migration practising detection + rollback choreography reverting safely
```

## Interview checklist aggregator

transactions • repository delineation • migration strategy • locking consistency • rollback narratives • pools • sqlx explicit query ownership • interplay with architectural boundaries forward Area 11

Deliverable emphasises retrospective write-ups—“**why remediation structurally healed class of defect**”—not ephemeral hotfix mysticism anecdotes.
