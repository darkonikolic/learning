# Unit 7 — Migrations hygiene: repeatable `up` / `down` rehearsal discipline

Operational truth:

Production abhors improvised `ALTER` improvisations undocumented—migrate scripts reviewed like auth code guarding money.

Practice migration adding column illustrative:

```
orders.country (nullable rollout strategy pondering expand-contract glimpses bridging zero-downtime awareness external cross-program references optionally)
```

Rehearse downgrade path—even if ephemeral dev DB nuking acceptable—you still author reversible intent expressing operational seriousness.

Interview prompts: articulate coordination with application dual-read/write phases when uncompromisable uptime constraints realistic—defer deep pattern mastery referencing external dedicated units if enriched elsewhere purposely.
