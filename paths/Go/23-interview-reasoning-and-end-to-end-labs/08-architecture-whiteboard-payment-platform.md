# Unit 8 — Architecture whiteboard: payment platform services

Whiteboard (paper/digital) these services and their interactions:

```
API
payment
inventory
worker
notification
```

Annotate:

```
queue usage
retry/timeouts ownership
idempotency surfaces
observability hooks
data ownership boundaries (who owns which tables/events)
```

Voice-over ≤5 minutes without reading notes—record yourself if helpful.
