# Unit 6 — Distributed systems scenario lab: payment succeeds, inventory fails

Narrate and document:

```
retry policy (when safe vs dangerous)
idempotency & dedupe story
queue handoff / saga compensation framing (Areas 12/14)
eventual consistency symptom vs bug distinction
```

Deliverable: architectural note + sequence outline (textual fine) + explicit **open questions** you’d ask stakeholders.
