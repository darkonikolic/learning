# Unit 6 — Capstone sketch: `api → Deployment → Service → Ingress`

Deliver a single cohesive diagram + short narrative describing:

```
how users reach your API
how rolling updates interact with readiness probes
where timeouts are configured (ingress vs service vs app client)
how you would surface golden signals (latency/errors/saturation) for the API’s SLO story (even if panels are imaginary)
```

Treat this as interview whiteboard rehearsal, not a Helm chart competition.
