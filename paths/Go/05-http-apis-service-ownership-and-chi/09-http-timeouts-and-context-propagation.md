# Unit 9 — Per-request timeouts & propagated context narrowing

Practice embedding synthetic slow payment collaborator sleeping beyond acceptable SLA enforcing earlier abort via tightened derived context timelines.

Demonstrate cooperating cancellation signals reaching nested repository fakers honouring **`ctx.Done()`**—even if repos still ephemeral memory until Area 08 deepens Postgres realism.

Enumerate strategies when legacy dependency wrappers lack native `context` signatures—adaptation façade sketch patterns minimally.

Interview drill: summarise failure classification when client disconnects prematurely mid-handler—observe possible wasted work nuances articulating politely.
