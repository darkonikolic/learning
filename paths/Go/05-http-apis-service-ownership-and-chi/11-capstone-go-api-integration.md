# Unit 11 — `go-api/` capstone integration consolidation

Mandatory route surface practising integrated themes:

```
GET /orders
POST /orders
GET /products
GET /health
```

Stack obligations recap:

```
chi routing
explicit validation middleware / handler interplay
structured logging keyed by request/trace correlation fields
timeouts contextually aligned
explicit error envelopes / mapping strategy succinctly documented
middleware ordering rationale appendix short note
```

## Deliverables (documentation-weighted—not code dump fetishisation)

Articulate succinctly:

```
ownership layering chain
anticipated failure taxonomy example table (even if partial)
latency budget sketch commentary at high-level
upgrade evolution hook (serialization/gRPC bridging forward Area06)
```

Interview consolidation checklist previously scattered—must verbally chain story uninterrupted simulating onboarding walk-through.
