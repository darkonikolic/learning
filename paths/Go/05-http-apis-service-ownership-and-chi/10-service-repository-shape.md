# Unit 10 — Service + repository seam rehearsal (thin handler religion)

Formalise layering narrative:

```
Handler (HTTP glue)
  → Application service orchestrating workflows + invariants bridging multiple repos eventually
       → Repository abstractions encapsulating persistence soon (temporary in-memory permissible learning stepping stone if documented transitional honesty)
```

Refactor drifting fat handlers surgically—even if simplistic domain rules currently.

Interview alignment previewing DDD-heavy Area 11 but pragmatic now: avoid cargo-cult aggregator services empty pass-through blindly—justify added layer cognitive tax via upcoming multi-repository coordination expectations honestly.
