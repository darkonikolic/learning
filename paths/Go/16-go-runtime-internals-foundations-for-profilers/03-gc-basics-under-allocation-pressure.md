# Unit 3 — Garbage collection under the profiling lens

Connect GOGC semantics at high altitude (defaults, throughput vs RSS trade knobs) strictly as **experiment with measurement**—not production tuning folklore from one laptop sample.

Practice reading **allocation-driven GC overhead** interplay using `pprof` + optional `gctrace` qualitatively.

Cross-link Areas `15`/`23` for deeper dives—avoid duplicating false-sharing micro-arch here unless bridging sentences only.
