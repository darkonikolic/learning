# Unit 3 — Interfaces enabling focused doubles (`gomock` or minimal hand mocks)

Practice isolating **`OrderRepository`** collaborator via mocks verifying orchestrating service behaviour—not SQL mechanics simultaneously—layering assertion clarity thoughtfully.

Enumerate mock over-proliferation pathology creating brittle suites mirroring implementation not contract—articulate heuristic mock boundaries verbally.

Bridging concurrency tests interplay awareness minimal mention—defer heavy synergy unless curiosity drives optional stretch.
