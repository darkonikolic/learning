# Unit 4 — Benchmark framing + exploratory load runs + fuzzing awareness groundwork

Operational commands literacy:

```
go test -bench
optional lightweight k6 scripted spike if curious—not mandatory core now
fuzz targets via go test fuzz harness for parsers / serializers prone edge combinatorics
```

Interview expectation: articulate interpreting noisy micro-benchmark deltas cautiously distinguishing signal vs ephemeral CPU frequency thermal noise honesty.

Discuss load testing aligning realistic workload modelling vs hello-world unrealistic QPS vanities debunk politely.
