# Unit 5 — Capstone: pragmatic coverage pyramid aligning REST-ish service artefacts

Synthetic target heuristic from original authoring brief:

```
strive balanced >80% coverage on critical packages if metrics meaningful—not gaming via trivial asserts
paired integration slices exercising repository + transactional boundaries hot paths
minimal benchmark artefacts anchoring hotspots candidates future optimisation—not premature micro-optimisation fetish formation
```

## Deliverable artefact expectation

Produce **Testing Strategy Markdown** succinctly aligning CI gates you’d propose defensibly onboarding team hypothetically—even if pipelines not enforced literally now.

Enumerate metrics anti-patterns (coverage mandates encouraging nonsense tests) guarding intellectual integrity politely.
