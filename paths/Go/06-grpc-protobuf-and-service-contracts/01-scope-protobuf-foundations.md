# Unit 1 — Placement & protobuf linguistic foundations (`proto3` baseline)

> **Bridging cadence cue:** dedicate ~4–6 sustained blocks AFTER HTTP ergonomics matured—ordering intentional.

Proto snippet orientation:

```
message User {
  int64  id = 1;
  string name = 2;
}
```

## Learning trajectory

Interpret **protobuf** contrasts JSON verbosity / dynamic typing ergonomics externally vs binary efficiency + schema coupling obligations internally.

Practice crafting evolving **`order.proto`** exercising:

```
messages • enums • repeated collections • cautious optional-ish fields respecting applicable proto version semantics precisely (verify toolchain docs!)
```

Maintain cross-language evolution empathy—even if downstream consumers hypothetical now.

Interview primer contrasts protobuf vs JSON not absolutism—articulate ambiguity debugging trade-offs honestly.
