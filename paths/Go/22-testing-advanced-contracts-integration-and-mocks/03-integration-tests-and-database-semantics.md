# Unit 3 — Integration testing strategy for repositories & databases

Move beyond “unit tests only” when SQL semantics matter:

```
transaction boundaries
constraint behaviour
migration compatibility smoke tests
```

Articulate cost: slower tests, flakiness risks—mitigate with containers or disciplined local fixtures.
