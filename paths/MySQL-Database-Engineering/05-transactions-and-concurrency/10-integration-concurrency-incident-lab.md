# Unit 10 — Integration lab (scale + failure classes)

Model large order history; inject deliberate deadlock scenarios, race regressions, locking hotspots.

Document detection (`SHOW ENGINE INNODB STATUS` historical patterns, modern performance schema usage per version) and corrective stories.

Interview recap: ACID, MVCC, isolation defaults on MySQL, phantom/repeatable read framing, pessimistic vs optimistic, deadlock strategy.
