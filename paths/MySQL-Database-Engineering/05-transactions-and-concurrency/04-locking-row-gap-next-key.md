# Unit 04 — Lock vocabulary (row, gap, next-key intention)

Exercise `SELECT … FOR UPDATE` producing blocking second writer updates.

Map gap / next-key locking purpose for phantom protection in RR (engine-specific—cite references you consult).

Interview: describe waiting vs deadlock distinction at conceptual layer.
