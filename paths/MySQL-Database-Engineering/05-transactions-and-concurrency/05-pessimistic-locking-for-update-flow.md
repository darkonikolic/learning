# Unit 05 — Pessimistic locking in checkout flows

Serialise inventory mutation using `FOR UPDATE` path; measure contention.

Interview: pessimistic applicability when collisions frequent or business tolerates fewer optimistic retries.
