# Unit 06 — Optimistic locking via version counters

Add `version INT` pattern; craft concurrent updates violating version predicate to surface application-level retry story.

Contrast with pessimistic friction under high churn.
