# Unit 03 — MVCC snapshots

Dual-session read/write interplay illustrating consistent reads without necessarily blocking writers (nuanced with locking reads later).

Link undo log / version chain intuition at high level.

Interview: why plain `SELECT` often avoids classic X-lock mutual exclusion compared to lock-in-mode reads.
