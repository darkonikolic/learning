# Unit 05 — Partitioning strategies (native table partitions)

Use large log-style tables (`audit_logs`) to reason about RANGE / HASH partition sketches.

Articulate scenarios where partitioning adds operational burden without payoff (tiny tables, uneven hot partitions).

Interview compare **partition vs shard** succinctly—scope of coupling, routing, cross-shard joins.
