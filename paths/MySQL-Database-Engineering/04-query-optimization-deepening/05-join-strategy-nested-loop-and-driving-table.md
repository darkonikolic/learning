# Unit 05 — Join strategy narration

Walk multi-table joins across `users` / `orders` / `payments` variants.

Discuss nested-loop mental model on InnoDB workloads, notion of driving table hypothesis, cardinality steering order.

Interview: pragmatic contrast with hash joins if engine introduces them later—stay version aware.
