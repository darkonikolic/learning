# Unit 03 — Sorting spill: Using filesort

Detect `ORDER BY` without aligning index ordering at large cardinality.

Iterate micro-benchmark introducing supportive composite index aligning sort keys.

Interview: articulate cost of external sort versus streaming index order.
