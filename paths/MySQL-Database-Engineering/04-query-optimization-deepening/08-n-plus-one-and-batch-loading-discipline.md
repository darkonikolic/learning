# Unit 08 — N+1 pathology and batching discipline

Simulate ORM-style repeated single-row fetch sequences (Doctrine-like mental model) versus join or batched strategies.

Measure round trips / statement counts (even crudely).

Interview: eager vs lazy loading trade-offs; API layer fan-out responsibility.
