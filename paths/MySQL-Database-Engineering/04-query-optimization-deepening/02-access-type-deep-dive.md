# Unit 02 — Access type continuum (prefer tight probes)

Understand favorable classes (`const`, `eq_ref`, `ref`, `range`) vs broader (`index`, `ALL`).

Build paired good/bad predicates demonstrating transitions.

Interview: rationale that `ALL` is not universally evil but alarming at scale without compensating tactics.
