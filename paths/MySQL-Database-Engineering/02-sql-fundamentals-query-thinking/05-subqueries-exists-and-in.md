# Unit 05 — Subqueries, correlated forms, EXISTS / NOT EXISTS

Patterns:

Users without qualifying orders.

Catalogue items lacking purchase signals.

Ranking-style customer selections using subqueries (before leaning fully on windows).

Compare **semi-join intuition (`EXISTS`)** versus **membership checks (`IN`)** on shaped data where nullability lurks — document surprising tri-valued logic interplay.

Interview: correlated subquery amplification risks; when optimiser reshapes predicates (still reason from clarity first).
