# Unit 06 — Common Table Expressions (CTE) for clarity

Stack multi-step derivations (e.g., `monthly_sales` CTE feeding `top_customers` CTE) inside a single statement.

Lab: Refactor a sprawling nested subquery into chained CTEs — comment on readability gains and when indirection still maps to similar plans (engine/version dependent; keep notes honest).

Interview tension: clarity vs perceived materialisation myths — treat CTE as organisational first, measure second when performance matters.
