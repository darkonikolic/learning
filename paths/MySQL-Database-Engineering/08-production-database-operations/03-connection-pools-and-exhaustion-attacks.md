# Unit 03 — Connection management and pool exhaustion narratives

Discuss `max_connections`, pool sizing interplay with PHP-FPM or other worker concurrency models, lingering idle leases.

Produce a hypothetical incident brief: saturation without visibly slow SQL (connection storms / pool misconfiguration).
