# Unit 04 — CPU, memory, disk pressure correlations

Associate CPU hotspots with hashing/sorting, RAM with buffer/cache pressure thresholds, disk with IOPS + latency envelope breaches.

Observe heavy aggregate queries elevating measurable signals deliberately in lab.
