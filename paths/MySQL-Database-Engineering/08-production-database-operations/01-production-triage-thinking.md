# Unit 01 — Production mindset diagnostics ordering

Enumerate triage playbook: classify symptom → corroborating metrics → likely classes (CPU saturation vs IO vs locking vs backlog) rather than blindly vertical scaling reflex.

Sandbox inject slow query spike plus synthetic connection pressure if ethically possible on disposable instances.
