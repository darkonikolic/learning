# Unit 04 — Leftmost prefix rule interrogation

Index `(country, status, created_at)` — derive which predicates can leverage prefix segments.

Craft ~10 exploratory queries forecasting usable segments vs residual scans — verify.

Interview recap: reordering composites without measuring access patterns wastes opportunity.
