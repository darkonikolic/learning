# Unit 09 — Slow query lab (intentional harm)

Produce pathological combinations: absent index, mis-ordered composites, malignant `LIKE`, large grouping without supportive ordering keys.

Iterate `EXPLAIN` → hypothesis → corrective index/query adjustments → annotated journal.
