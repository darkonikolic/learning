# Unit 07 — EXPLAIN literacy (access types sanity)

Iterate ~20 candidate queries tagging `ALL`, `range`, `ref`, `const`, residual `filtered` guesses.

Diagnose ominous breadth (`ALL`), suspicious row estimates versus actuals when `EXPLAIN ANALYZE` available.

Interview clarify meaning of **`rows`** (estimate historically) versus measured timings.
