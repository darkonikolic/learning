# Unit 01 — Query optimisation interrogation drills

Given pathological predicates (functions on indexed timestamps, malignant `LIKE`, star projections)—produce rewritten SQL, justification sketch, hypothetical index deltas, residual risk disclosures.

Maintain a personally authored bank of optimisation micro-puzzles (target ≥20 over time).
