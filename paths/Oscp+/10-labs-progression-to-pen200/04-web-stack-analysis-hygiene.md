# Unit 04 — Structured web confrontation mindset (Symfony + JWT + OAuth + Burp)

Cross-pollinate HTB-aligned narrative courses (subset you elect) alongside **authorised local Symfony playgrounds**.

Trace narrative:

Plaintext / TLS HTTP surfaces → nuanced header interplay → brittle cookie choreography → JWT lifecycles glimpsed ethically → OAuth exchange cadence critiques → brittle authorisation branching reviews.
