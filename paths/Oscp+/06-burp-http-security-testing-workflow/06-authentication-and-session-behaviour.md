# Unit 06 — Authentication & session artefacts in motion

Parallel PortSwigger **Authentication + Sessions** lab clusters alongside **intercepted Symfony flows**.

Harvest (under authorisation):

- Lifecycle of session cookies (**flags**, **scopes**, rotation cadence hypotheses).  
- Header vs cookie token transport inconsistencies.  

Reason about MFA absence implications, brittle refresh cadence exposures, fixation class mistakes—conceptual depth first.

Deliverable snippet: enumerated **signals** distinguishing healthy vs suspicious session choreography on your sandbox metrics logs.
