# Unit 04 — How the web works (engineering + security vantage)

## Theme

Interpret HTTP-era surfaces before diving offensive labs later.

## TryHackMe — How The Web Works (cluster)

Work modules approximating detailed DNS traversal, granular HTTP anatomy, lifecycle “putting it together” syntheses—adapt to refreshed naming.

Browser DevTools — **Network** tab session:

Observe at least:

- Selected request headers & response baseline  
- Status code lineage on redirect chain benign site  
- Cookie emission & scope concepts (don't memorize vendor quirks early)  

## Guided observation

Navigate an intentionally simple static site versus a heavyweight SPA minimally—feel difference in chatter volume—not deep front-end apprenticeship.

## Learning outcome

You explain where application trust incorrectly assumes sanitized client assertions—foundationally.
