# Unit 07 — OAuth 2 / OIDC attack surfaces

Labs: lax redirect URIs, `state` omission, leakage via referrers/history/logging, brittle implicit-era anti-patterns if modules still illustrative.

Comfort narrative (no exam trivia): Authorization Code **`+`** PKCE for public clients rationale, refresh token stewardship, whitelist discipline for redirect URIs, careful logging redaction stance.

Symfony integration audit: OAuth client configs & callback verification rigor—even stub environments.
