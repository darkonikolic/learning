# Unit 12 — OWASP taxonomy mapping rehearsal

Produce **personal** mapping rehearsal (adapt naming to current Top 10 cycle if OWASP numbering shifts—verify headings online):

Examples of mapping style (pattern—not immutable gospel):

SQLi ⇒ Injection class

Dominant XSS classes ⇒ Injection (context nuance annotate)

Broken auth/JWT ⇒ Authentication failures cluster

Broken OAuth redirect discipline ⇒ Broken access control overlaps + authentication choreography failures

Dependency findings ⇒ Vulnerable components

SSRF ⇒ dedicated SSRF row / server-side abuse cluster

Produce per-vulnerability rehearsal row:

Attack intuition → Detective signal framing → Mitigation strata → OWASP anchor → Local reproduction pointer (ethical)

Expect iteration—this spreadsheet mentality matures—not one-and-done cram.
