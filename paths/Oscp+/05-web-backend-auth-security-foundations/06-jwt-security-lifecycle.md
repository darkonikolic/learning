# Unit 06 — JWT security & verification discipline

Labs: weak secrets, naive `alg` confusion classes, omission of signature enforcement stories—Academy-guided only.

Operational checklist mental model claims `iss`, `aud`, expirations/skew defenses, JWKS rollover tolerance, revocation partial realities.

Bridging hygiene: bcrypt/argon adaptive hashing at password ingress edges—distinct topic but intersects naive token piggybacking anti-patterns.

Local inspection: JWT middleware validation fidelity on sample tokens you mint intentionally flawed in dev.
