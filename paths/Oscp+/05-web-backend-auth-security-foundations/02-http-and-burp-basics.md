# Unit 02 — HTTP in depth via Burp basics

## Theme

Operational HTTP literacy through deliberate interception.

PortSwigger: primer units on HTTP request/response grammar, verbs, evolving header roles, cookie mechanics, numeric status taxonomy—progress until Proxy confidence solid.

Burp competencies:

Activate **Proxy intercept** ethically on local target only.

Explore **Repeater** iteration discipline.

Harvest **history** selectively—avoid hoarding unrelated sensitive noise.

Symfony/local API micro mutation:

```http
GET /users?page=1
```

Iterate parameter boundary tests you hypothesize—not random fuzz spam without intent.

Topics: verbs, redirection trust, HTTPS termination awareness, framing future abuse avenues abstractly only.
