# Unit 05 — Sessions, authentication hardening & CSRF

Academy arcs: Authentication vulnerability cluster + CSRF patterns.

Themes: brute-force rate friction, MFA absence implications, flawed password reset choreography classes, fixation risks, logout invalidation fidelity, predictable tokens, predictable sessions.

Mitigation shorthand: CSRF synchronization token patterns (`SameSite` cookie augmentation mental model—not sole defense fantasy).

Symfony audit angles: integrated CSRF token usage on state-changing routes, lifecycle & rotation strategies, sane idle & absolute timeouts if applicable.
