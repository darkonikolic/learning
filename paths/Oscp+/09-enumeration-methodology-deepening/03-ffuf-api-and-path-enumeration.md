# Unit 03 — Directed `ffuf` on local API facets

Apply tight wordlists to paths such as `/api/FUZZ` on authorised Symfony scaffolding.

Combine `curl` + JSON tooling with `ffuf` output deltas to classify anomalies deliberately.
