# Unit 07 — `enum4linux` & LDAP-adjacent triage etiquette

Install per distro guidance (`sudo apt install enum4linux` etc.) — **targets must remain systems you erected**.

Harvest **sanitised** summaries: users/group hints, SMB pipe enumerations glimpses—paired with handwritten hypothesis notes.

Cross-link early LDAP rehearsal (`ldapsearch` style queries) pointing only inward.
