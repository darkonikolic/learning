# Unit 05 — bcrypt / Argon2 awareness & sanctioned cracking rehearsals

Demonstrate illustrative PHP hashing:

```php
password_hash('ExampleSecret!', PASSWORD_BCRYPT);
```

Discuss work-factor tradeoffs versus Argon2 class algorithms.

Introduce **John** / **Hashcat** workflows **only on synthetic hashes you generated** inside the lab—never public breach material.
