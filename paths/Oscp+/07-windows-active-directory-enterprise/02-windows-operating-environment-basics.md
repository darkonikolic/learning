# Unit 02 — Windows system fundamentals recap

Operational drills (elevated responsibly inside your VMs):

PowerShell glimpses (`Get-Process`, `Get-Service`, `hostname`, `whoami`, `ipconfig /all`)

Classic shell companions (`tasklist`, `net user`, `hostname`).

Enumerate baseline mental map: filesystem layout intuition, servicing model, rudimentary ACL awareness, interactive vs noninteractive session nuance—no exploit catalogues yet.
