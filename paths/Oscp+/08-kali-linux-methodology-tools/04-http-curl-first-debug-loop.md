# Unit 04 — CURL-first reasoning on sanctioned APIs

Examine illustrative Symfony surfaces:

```bash
curl -sv http://localhost:8080/version
curl -X POST ...    # ethically crafted bodies only on owned fixtures
curl -o /dev/null -D - ...
```

Interpret status families, deceptive redirects, chunked anomalies—pair with structured JSON pretty viewers when payloads warrant.
