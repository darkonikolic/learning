# Unit 08 — OAuth flows from browser vantage through proxy logs

Observe holistic chain:

browser engagement → authorised redirect choreography → ephemeral code exchange phase → bearer usage on API—all under lab consent.
