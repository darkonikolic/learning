# Unit 03 — Authorised depth scanning with `nmap`

Operate exclusively against VMs you erected (e.g. local Symfony `php -S` fixture).

Iterate flags discussed in syllabus order—internalise sequencing:

presence sweep → versioning curiosity → scripted safe defaults escalating only with explicit approval inside your tenancy.

Articulate rationale for SYN vs connect scans at high level—not raw memorisation devoid of IDS awareness discussion on shared networks (**avoid shared Wi-Fi experiments** causing collateral noise).
