# Unit 06 — Precision fuzz placeholders via `ffuf`

```bash
sudo apt install ffuf
ffuf -u http://localhost:8080/FUZZ -w wordlist.txt
ffuf -u http://localhost:8080/api/FUZZ ...
```

Maintain **narrow wordlists**, rate discipline, conscientious concurrency—ethical labs only.

Document mis-hit patterns illuminating hidden enumerations—not merely status-code bingo.
