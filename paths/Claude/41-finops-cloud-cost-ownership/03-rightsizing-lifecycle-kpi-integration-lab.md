# Rightsizing • lifecycle storage • KPI scoreboard integration lab

Iterate candidate rightsizing backlog with **risk class** tagging (cheap experiment vs nuanced perf regression).

Cold storage tiers & ephemeral env TTL linkage to platform (`38`).

### KPI mini-board

Monthly capture: infra cost trajectory vs **released value proxy** realism (avoid vanity normalisation fantasies—but force explicit assumption).
