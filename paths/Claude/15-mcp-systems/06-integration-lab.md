# Integration lab — MCP systems orchestration

Composite artefact resembling micro-platform stress:

Symfony API strand  

Go worker consumer  

MySQL datastore  

Dockerised runtime façade  

Terraform (or compatible IaC) footprint

Synthetic fault bouquet options:

-queue throughput decay  

-payment **refund** failures  

-schema / migration fallout

Produce explicit orchestration dossier marrying earlier templates:

```
 problem framing anchored in observed signals (tools first)
       → ROUTING rationale across filesystem / git / DB / shell / browser as needed
             → PERMISSION recap (credential class exposure audit)
                   → CAPABILITY limits acknowledged (coverage gaps enumerated honestly)
                         → investigative timeline with validation gates
                               → restrained fix proposal
                                     → rollback / recovery pairing
```

### Measure & retrospect

Evidence **quality**, correct **tool choice frequency**, needless **iteration counts**, rework after bad routing.

Notebook asks:

Where assistants **picked incorrect modality** prematurely  

Capability blind spots amplified thrash  

Routing graph adjustments you will codify personal rule-side next iterations

Checkpoint closure: habitual mental prompt becomes **multi-tool choreography design** before freeform answer generation.
