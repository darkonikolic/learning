# Planner agent

**Theme:** Planner materialises navigable futures—dependencies explicit—without touching implementation noise.

Operational chain:

```
 goal restatement sharpening
       → decomposition ladder (vertical slices—not megatasks)
               → precedence / parallelism classification
                             → prioritisation aligning risk & dependency gravity
                                   → READY markers for downstream roles
```

**Forbidden pattern for labs:** Planner pseudo-code creeping into idiomatic commits—enforce separation discipline.

Practice canvases:

Go **payment worker** lifecycle refactor—ordered steps isolating behavioural vs infra toggles  

Symfony **CQRS flow** uplift sequencing command vs projection migrations  

Terraform **deployment wave** structuring—state checkpoints between blast phases

Mandatory lab prohibition echo: Planner output **never** substitutes Implementer textual code slabs—plans may cite file targets & acceptance hooks only.

### Checklist

- [ ] Planner outputs reference **verification hooks** QA will replay—not vague “finish feature.”  
