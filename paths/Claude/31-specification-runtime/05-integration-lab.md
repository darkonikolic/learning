# Integration lab — specification runtime closure

Unified **payments platform** rehearsal crossing Symfony surfaces, Go services/workers messaging through **RabbitMQ**, relational **MySQL** truth, containerised footprints (**Docker**), plus Claude automation discipline.

Mandatory pipeline explicitly executed end-to-end:

```
 SPEC freeze (excerpt + acceptance + constraints + NFR)
      → IMPLEMENT (respect frozen edges)
            → VERIFY (functional + structural)
                  → CONSTRAINT check (exceptions logged if any)
                        → NFR check (defer only with tracked ticket)
                              → drift scan → bounded REPAIR loop
                                    → archived acceptance attestation
```

### Deliberately stress (sandbox)

Tiny numeric drift (timeouts) creeping  

Constraint breach attempt (disallowed dependency) phantom introduction  

Silent NFR absence mid-build rediscovered late—measure detection latency organisational learning

Measured outcomes:

Holistic qualitative **quality**  

**SPEC drift incidence** tally  

Repair iteration counts  

Explicit **constraint adherence** ratio final pass stage

Notebook honesty sections:

Contexts assistants **lost spec fidelity** prematurely  

Fragile verification surfaces needing automation next iteration  

Structural reasons **verification remained shallow** inadvertently

Checkpoint closure affirmation habitual narrative evolves from Claude **authored opaque code blobs** toward Claude **implementing audited specifications with provable conformance discipline** sustainably.
