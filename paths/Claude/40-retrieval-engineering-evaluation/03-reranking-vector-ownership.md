# Reranking • vector store ownership discipline

Operational layers: lexical prefilter → semantic recall → rerank refinement—where compute budget caps live.

Vector DB vigilance topics: compaction, duplication drift, deletion correctness (GDPR analogue awareness), latency tail sensitivity.
