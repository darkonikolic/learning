# Golden paths & self-service infrastructure

### Golden path decomposition

Starter kit components: scaffolding, lint/test baseline, observability instrumentation hooks (`35`), secrets bootstrap (`37`).  

Declare **variation budget** («supported extension points»)—where customization must route through platform backlog.

### Self-service safeguards

Namespaces / clusters / quotas with **policy-as-code gates** preventing silent cost & security divergence.

### Deliverable blueprint

One golden path README («from idea to observable deployment»)—even if hypothetical.
