# Permission ownership

**Theme:** A capability existing ≠ assistant **authorised** to wield it blindly—map **explicit permission strata** per modality.

Survey modalities rehearsed explicitly:

Filesystem reads vs writes spanning sensitive roots  

Git history vs rewriting operations disallowed ethically  

Database introspection vs destructive DDL default posture **forbidden unattended**  

Shell / MCP terminal destructive command classes behind approval matrix parity with prior security syllabus

### Per-tool mini-table LAB

Populate **SAFE / NEEDS_APPROVAL / FORBIDDEN** rows—with concrete examples—not abstract optimism.

Discuss **approval model** interplay: organisational identity boundaries may narrow tokens further than IDE defaults casually—document overrides.

Discuss **routing ownership mismatch**: MCP server accidentally mounted too wide filesystem path—architecture threat—periodic audits not only policy documents.

Leakage vigilance: tool outputs inadvertently echo secrets—still **verification + redaction** obligations persist.

### Checklist

- [ ] Permission table references **credential class** exposures explicitly—not only boolean connect/disconnect metaphors vaguely.  
