# Parallelization strategy

**Theme:** Serious orchestrations accelerate via **controlled parallelism**—only where **dependency DAG** allows safely.

Illustrative parallel fan-out personas:

Planner decomposition  

Architect structural slice  

Reviewer / QA heuristic pre-pass scaffolding

Claude-assisted stack must nevertheless:

Measure aggregated **latency** deltas truthfully—not only wall clock cheerleading ignoring merge conflicts bursts  

Monitor **repair count** fusion collisions cause—parallel drafts may diverge spec semantics silently absent merge discipline strictly  

Guard composite **quality** via merge acceptance rubric—not race-to-first-answer alone

Discuss **agent cost ownership**: parallel runs multiply billed steps—financial approvers aware when orchestration extravagance unjustified ethically.

Discuss **serialization necessities**: SPEC freeze handshake, shared state merges, infra approval dependencies—DAG clarity prevents harmful pseudo-parallel thrash pretending efficiency.

Discuss **evaluation**: record whether parallel lowered iteration count materially—hypothesis falsifiability prized.

Mandatory lab journaling: annotate **branching parallelism plan** referencing ownership & checkpoints before spawning concurrent assistant lanes ethically.

### Checklist

- [ ] Parallel fan-out artefacts reconcile through **explicit merge steward role** preventing divergent contradictory architecture truths silently coexisting dangerously.  
