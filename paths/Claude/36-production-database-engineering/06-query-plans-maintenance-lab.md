# Query plan ownership, slow queries & maintenance posture

### Slow query ownership (process)

Who **classifies**, who **prioritizes**, regression guard after rollout—pair with **`11-go-performance`** & Symfony query audit habits.

### EXPLAIN discipline

Cardinality estimates lies you accept; **histogram freshness** caveat; intentional index addition vs workload rewrite ROI.

### Maintenance / housekeeping

Operational calendar: backups proof restore, archiving, compaction analogues relevant to engine—tie to **risk register** (`07` workflows).

LAB: Maintain a **Monthly DB Health Snapshot** artefact template (bullet page: top slow, top contested tables, anomalies).
