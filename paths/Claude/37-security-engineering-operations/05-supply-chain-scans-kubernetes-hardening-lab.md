# SBOM • supply chain • container & scan pipelines • K8s RBAC • NetworkPolicy

### SBOM stance

Generation points (build), ingestion into registry/policy gate (conceptual)—**risk acceptance** documented when tooling partial.

### Supply chain layering

Signed images / provenance class awareness—even if phased adoption.

### SAST • DAST balance

Early signal vs noisy triage backlog—coverage targets vs diminishing returns realism.

### Image scanning runtime security

Baseline hardening checklist (readonly rootfs class mitigations)—contextual allowances.

### K8s RBAC & NetworkPolicy crosslink

Minimal privilege service accounts tied to workloads; egress policy guardrails aligning **`13`** deepening.

### Integration lab

Map one critical deploy path: **pipeline stages → scanners → rollback gate** annotated.
