# Unit 2 — Labs: scripted pressure scenarios

## Week-style rotation (adapt pacing freely)

1. **Event-loop worksheet**: annotate console order for layered `microtask`/timer puzzles; verbalise closures holding live memory across scopes.
2. **Type-level incident**: widen a union accidentally in a mapper; restore exhaustiveness; explain why `any` hid a production bug.
3. **Reactivity/debug**: sluggish dashboard — hypothesis list + Vue DevTools plan before editing code.
4. **SPA architecture whiteboard**: draw ownership slices for ecommerce routes/stores/services.
5. **API failure tabletop**: timeouts + rollback of optimistic UX with user-safe messaging plan.
6. **Production tabletop**: stale JS + phantom API success + UI timeout — differentiate layers.
7. **Performance tabletop**: huge dataset + sluggish bundle — backlog of mitigations prioritised by risk/effort.
8. **Enterprise capstone (~90 min silent)**: Symfony API + SPA + cascading symptoms — document investigation trail only (no spoilers in chat transcripts).
9. **Final scavenger hunt on `shop-spa/`**: inject intersecting defects (auth, retry, stale cache, rerenders, leaks) → single RC write-up capturing root cause vs symptom.

Interview checklist echoes original roadmap breadth: closures & event loop → **TypeScript narrowing & contracts** → Vue through deploy/CSP/cache/monitoring narratives.
