# Unit 2 — Labs: mini-threat model → control ledger

Reuse a real service outline (payments API, onboarding, messaging).

Exercise **diagram**: annotate trust boundaries **Client → CDN → LB → service → Postgres / queue / third-party webhook**.

Exercise **stride-lite**: catalogue at least eight threats spanning spoofing → elevation; keep each line one sentence causal.

Exercise **controls map**: authentication, authorisation, secrets, transport encryption, at-rest stance, auditing, quotas—each paired to mitigated threats with residual risk honesty.
