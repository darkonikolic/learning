# Unit 2 — Labs: scale phases & amplification stories

Lab A — **Three horizons**  

Same product caricature (**small / grown / strained** growth). Record which subsystem becomes the bottleneck at each horizon and why (state, IO, concurrency, organisational complexity).

Lab B — **Read vs write stress**  

Compare two bursts: spike of **analytics reads** vs spike of **order writes**. Decide whether replicas, caching, queue ingestion, partitioning, or process change addresses each—explain mis-scaling pitfalls.

Interview drill: when would replicas **hurt** correctness or customer trust if staleness underestimated?

