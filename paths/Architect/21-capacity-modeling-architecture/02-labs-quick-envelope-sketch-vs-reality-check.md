# Unit 2 — Labs: envelope arithmetic drill

Assume public read-heavy JSON API averaging 700 RPS baseline, burst 10× weekday mornings.

Exercise **spreadsheet-lite** (mental): requests → instances assuming CPU/bind profile you state explicitly.

Exercise **risk row**: cite what invalidates simplistic division (cold caches, retries, hotspot keys).
