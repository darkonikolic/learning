# Unit 2 — Labs: analytics pipeline tabletop

Assume product events → warehouse + anomaly dashboard SLA T+30m.

Exercise **diagram**: ingestion, buffering, compaction, serving layers.

Exercise **latency integrity**: reconcile duplicate events, watermarking metaphors—even if terminology vendor-specific abstractly.

