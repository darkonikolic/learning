# Unit 1 — Scope: advanced API ergonomics

Connect gateway patterns, versioning discipline, pagination strategies, eventual consistency signalling, caching headers, resilience policies.

---

## Architectural outcomes

Describe **gateway** responsibilities versus service-native concerns (routing, aggregation, coarse authZ, quotas).
Explain **backward compatibility**, deprecation windows, telemetry contracts for breaking changes.
Enumerate **pagination** trade-offs (cursor vs offset) for large evolving datasets.
Articulate eventual consistency signalling to clients responsibly.
Enumerate cache header semantics interplay with gateways / CDNs.
Outline rate limiting tiers and degraded-mode exposure strategies.

