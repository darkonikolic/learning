# Unit 2 — Labs: gateway layering & versioning drill

Assume public REST API at scale.

Exercise **diagram**: annotate where gateway terminates TLS, attaches identity, aggregates calls, emits metrics.

Exercise **pagination choice**: argue cursor vs offset for an append-only ledger feed; state failure scenarios.

Exercise **deprecation tabletop**: staged sunset with dual-run metrics and client signalling plan.
